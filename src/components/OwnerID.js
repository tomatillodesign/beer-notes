import React from 'react';

const OwnerID = (props) => (
     <div className="not-found">
          <div>OwnerID via custom test component: {props.ownerID}</div>
     </div>
);

export default OwnerID;
